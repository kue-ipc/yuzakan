require_relative '../../../spec_helper'

describe Web::Controllers::Password::Edit do
  # let(:action) { Web::Controllers::Password::Edit.new }
  # let(:params) { Hash[] }

  # it 'is successful' do
  #   response = action.call(params)
  #   _(response[0]).must_equal 200
  # end
end
