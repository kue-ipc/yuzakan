# frozen_string_literal: true

require_relative '../../../../spec_helper'

describe Web::Controllers::Gsuite::Lock::Destroy do
  let(:action) { Web::Controllers::Gsuite::Lock::Destroy.new }
  let(:params) { Hash[] }

  # it 'is successful' do
  #   response = action.call(params)
  #   _(response[0]).must_equal 200
  # end
end
