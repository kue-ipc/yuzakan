<%== params_to_json(Yuzakan::Utils::Hash.compact_blank({
  status: status&.to_h,
  location: location&.to_s,
  csrf: csrf.compact,
  flash: flash.sweep.map(&:itself).to_h, # rubocop: disable Style/MapToHash
})).delete_suffix("}") %>,"data":<%== yield %>}
